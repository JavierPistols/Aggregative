#pragma once

//#include "Individual.h"
//#include "DB.h"
//#include "Smethods.h"
//#include "Population.h"


class Manipular
{
public:
	Manipular();
	~Manipular();
	/*
	static float fitGame(int position, DB & games);
	static float evalFunc(Individual & evaluated,DB & games);
	static void mutation(Individual & mutant);

	static void fillUP(Population * newy,Population * oldy);
	static  Individual selection(Population & pop);
	static void sort(Population & pop);
	static void printVec(Population & pop);
	static pair<Individual, Individual> crossing(Population & pop);*/
};



